function generateLEDMovingPattern(
    direction: PatternDirection,
    binCode: number
): string {
    var s = ""
    for (let i = 0; i += 1; i++) {
        let base = "0"
    }
}

enum PatternDirection {
    Up, Down, Left, Right
}